(define (contains-length? lst)
    (define (contains-length-helper l remaining)
        (cond
            ; base case: remaining is empty so I return true
            [(empty? remaining) #t]
            
            ; case 1: 
            ))
    (contains-length-helper (length lst) lst))
